// Shared constants — loaded before content, popup, and background scripts.
// Uses var so the binding is a true global (accessible via window/globalThis).
var WST_NS_STORAGE_KEY = 'wstNsEnabled'; // eslint-disable-line no-unused-vars
